services:
  srf-web:
    build:
      context: ..
      dockerfile: cloud/Dockerfile
    ports:
      - "8000:8000"
    environment:
      SRF_JWT_SECRET: "troque-este-segredo"
      SRF_USERS: "isaac:senha123,usuario2:senha123,usuario3:senha123"
      SRF_DEFAULT_MODE: "standard"
      SRF_USE_AZURE_BLOB: "0"
    volumes:
      - ./sessions:/app/cloud/sessions
